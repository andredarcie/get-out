{
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "playwright": "^1.58.2"
  },
  "parcel": {
    "transformers": {
      "*.mp3": [
        "@parcel/transformer-raw"
      ],
      "*.wav": [
        "@parcel/transformer-raw"
      ]
    }
  }
}
