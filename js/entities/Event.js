import { Globals } from '../Globals.js';

export class Event {
    constructor(name, description, type) {
        this.name = name;
        this.description = description;
        this.type = type;
    }
}